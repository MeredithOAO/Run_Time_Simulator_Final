The compiler we use is Cl.exe.
You can edit the .vscode/tasks.json to edit the file that needs to be included.

To use the Cl.exe, you need to run the VS Code from a "Developer Command Prompt for Visual Studio".(Use the "code ." command)
